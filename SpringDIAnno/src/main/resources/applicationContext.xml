<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">
  <!--声明组件扫描器(component-scan)，组件就是java对象
  base-package:指定注解在你的项目中的包名
  component-scan工作方式: spring会 扫描逼历base-package指定的包，
  找到类中的注解,把包中和子包中的所有类，按照注解的功能创建对象，或给属 性赋值

  加入了component - scan标签，配置文件的变化:
  1.加入一个新的约束文件spring- context. xsd
  2.给这个新的约束文件起个命名空间的名称 context
  -->
  <context:component-scan base-package="com.xxx.ba02"/>
  <!--指定多个包，1 使用多个标签，2使用一个标签，不同包用分号隔开 3 指定父包-->

</beans>